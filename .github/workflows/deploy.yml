name: 🚀 CI/CD - Deploy JusCash API

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

# Workflow simplificado - Deploy direto na VPS sem registry

jobs:
  test:
    name: 🧪 Testes Automatizados
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: test_db
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4

    - name: 🐍 Setup Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 📦 Cache dependências
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: 📋 Instalar dependências
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov

    - name: 🔍 Lint com flake8
      run: |
        pip install flake8
        flake8 app/ --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 app/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: 🧪 Executar testes
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
        REDIS_URL: redis://localhost:6379/0
        SECRET_KEY: test-secret-key
        FLASK_ENV: testing
      run: |
        flask db upgrade
        pytest tests/ -v --cov=app --cov-report=xml --cov-report=term

    - name: 📊 Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella

  deploy:
    name: 🚀 Deploy to VPS
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    environment:
      name: production
      url: https://cron.juscash.app

    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4

    - name: 🔐 Setup SSH
      uses: webfactory/ssh-agent@v0.8.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: 📋 Add server to known hosts
      run: |
        ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

    - name: 🚀 Deploy to server
      env:
        VPS_HOST: ${{ secrets.VPS_HOST }}
        VPS_USER: ${{ secrets.VPS_USER }}
      run: |
        ssh $VPS_USER@$VPS_HOST << 'EOF'
          set -e
          
          echo "🔄 Iniciando deploy automático..."
          
          # Navegar para diretório do projeto
          cd /var/www/juscash
          
          # Backup da versão atual
          echo "💾 Fazendo backup (se banco disponível)..."
          docker-compose exec -T db pg_dump -U juscash juscash_db > backup_$(date +%Y%m%d_%H%M%S).sql 2>/dev/null || echo "⚠️ Backup falhou - continuando deploy..."
          
          # Pull das mudanças com limpeza forçada
          echo "📥 Atualizando código..."
          echo "🗑️ Limpeza forçada completa..."
          git stash drop || true
          git checkout -- .
          git clean -fd
          git fetch origin
          git reset --hard origin/master
          echo "✅ Código atualizado para: $(git log --oneline -1)"
          
          # Corrigir permissões após reset
          echo "🔒 Corrigindo permissões..."
          chmod +x docker-entrypoint.sh
          
          # Parar containers (tolerante a falhas)
          echo "⏹️ Parando containers..."
          docker-compose down --remove-orphans || echo "⚠️ Erro ao parar containers - continuando..."
          
          # Iniciar containers com rebuild forçado
          echo "🚀 Iniciando e rebuilding containers..."
          docker-compose up -d --build --force-recreate --remove-orphans || { echo "❌ Erro no up - tentando restart..."; sleep 5; docker-compose restart; }
          
          # Aguardar serviços
          echo "⏳ Aguardando serviços inicializarem..."
          sleep 20
          
          # Verificar se containers estão rodando
          echo "📊 Verificando containers:"
          docker-compose ps
          
          # Aguardar mais um pouco para aplicação inicializar
          echo "⏳ Aguardando aplicação inicializar..."
          sleep 20
          
          # Executar migrações se necessário
          echo "🗄️ Aplicando migrações..."
          if ! docker-compose exec -T web flask db upgrade; then
            echo "⚠️ Migrações falharam - continuando..."
          else
            echo "✅ Migrações aplicadas com sucesso"
          fi
          
          # Health check rigoroso
          echo "🏥 Verificando saúde da aplicação..."
          sleep 15
          
          echo "🔍 Tentativa 1/3 de health check..."
          if ! curl -f --max-time 10 http://localhost:5000/api/simple/ping; then
            echo "⚠️ Tentativa 1 falhou, aguardando..."
            sleep 10
            echo "🔍 Tentativa 2/3 de health check..."
            if ! curl -f --max-time 10 http://localhost:5000/api/simple/ping; then
              echo "⚠️ Tentativa 2 falhou, aguardando..."
              sleep 10
              echo "🔍 Tentativa 3/3 de health check..."
              if ! curl -f --max-time 10 http://localhost:5000/api/simple/ping; then
                echo "❌ DEPLOY FALHOU! API não responde após 3 tentativas"
                echo "📊 Status dos containers:"
                docker-compose ps || true
                echo "🔍 Logs do container web:"
                docker-compose logs web | tail -20 || true
                exit 1
              fi
            fi
          fi
          
          echo "✅ Deploy realizado com sucesso! API respondendo corretamente"
          
          # Mostrar status
          echo "📊 Status dos containers:"
          docker-compose ps
          
          # Verificar commit atual
          echo "📋 Commit deployado: $(git log --oneline -1)"
          
          # Limpar imagens antigas
          docker image prune -f
          
          # Manter apenas 5 backups mais recentes
          ls -t backup_*.sql 2>/dev/null | tail -n +6 | xargs -r rm || true
        EOF
        
        # Verificar se o comando SSH teve sucesso
        if [ $? -ne 0 ]; then
          echo "❌ Deploy falhou! Comando SSH retornou erro."
          exit 1
        fi
        
        echo "✅ Comando SSH executado com sucesso!"

    - name: 📱 Notify deployment status
      if: always()
      run: |
        if [ ${{ job.status }} == 'success' ]; then
          echo "✅ Deploy realizado com sucesso para https://cron.juscash.app"
        else
          echo "❌ Deploy falhou!"
        fi

# Notificações removidas - Discord não será usado 